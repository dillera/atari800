---
phase: 01-emulator-interface
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - puppetmaster.py
autonomous: false
requirements:
  - EMU-01
  - EMU-02
  - EMU-03
  - EMU-04
  - EMU-05
  - EMU-06
  - EMU-07
  - EMU-08
  - EMU-09

must_haves:
  truths:
    - "Running puppetmaster --launch killzone.xex starts atari800 with KillZone loaded and emulator window is visible"
    - "Running puppetmaster with no --launch connects to an already-running emulator and prints connection confirmation"
    - "Sending W/A/S/D via press_key visibly moves the player in the KillZone game window"
    - "After each key press the key is released (no stuck inputs)"
  artifacts:
    - path: "puppetmaster.py"
      provides: "Complete CLI + controller, tested end-to-end against live emulator"
  key_links:
    - from: "puppetmaster.py CLI --launch"
      to: "subprocess.Popen spawning ./src/atari800 -ai -xl -run"
      via: "PuppetmasterController.launch_and_connect"
      pattern: "subprocess\\.Popen"
    - from: "puppetmaster.py CLI --keys"
      to: "PuppetmasterController movement and action methods"
      via: "move_up/down/left/right/attack/menu_q/y/n calls"
      pattern: "move_up\\|move_down\\|move_left\\|move_right"
---

<objective>
Live end-to-end verification that Puppetmaster controls the KillZone client running in atari800.

Purpose: Confirm that keyboard inputs sent via puppetmaster.py visibly move the player on screen, keys are properly released, and the launch flow brings up a working emulator window. This is the acceptance gate for Phase 1.

Output: Human-verified confirmation that all Phase 1 success criteria are met.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-emulator-interface/01-CONTEXT.md
@.planning/phases/01-emulator-interface/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --keys smoke-test mode and --connect-only flag to CLI</name>
  <files>puppetmaster.py</files>
  <read_first>
    - /Users/dillera/code/atari800/puppetmaster.py (read full file — this modifies existing code)
    - /Users/dillera/code/atari800/.planning/phases/01-emulator-interface/01-01-SUMMARY.md (what was built in plan 01)
  </read_first>
  <action>
    Add two new flags to puppetmaster.py's argparse CLI:

    1. --keys (boolean flag): sends one press of each key (W, A, S, D, Space, Q, Y, N) with a 10-frame pause between each, printing each key name before sending it.

    Add to argparse: parser.add_argument("--keys", action="store_true", help="Send one press of each key (W A S D Space Q Y N) with 10-frame pauses")

    Behavior when --keys is set (add to main() after connection success):
    ```python
    if args.keys:
        key_sequence = [
            ("W", "move up", controller.move_up),
            ("A", "move left", controller.move_left),
            ("S", "move down", controller.move_down),
            ("D", "move right", controller.move_right),
            ("Space", "attack", controller.attack),
            ("Q", "menu Q", controller.menu_q),
            ("Y", "menu Y", controller.menu_y),
            ("N", "menu N", controller.menu_n),
        ]
        for key_name, desc, method in key_sequence:
            print(f"Sending {key_name} ({desc})...")
            method()
            controller._client.run(frames=10)
        print("Keys test complete.")
    ```

    2. --connect-only (boolean flag): exits immediately after confirming connection.

    Add to argparse: parser.add_argument("--connect-only", action="store_true", help="Connect to emulator, print confirmation, then exit")

    Behavior in main() — add this check right after printing "Puppetmaster ready. Emulator connected.":
    ```python
    if args.connect_only:
        sys.exit(0)
    ```

    Note: argparse converts --connect-only to args.connect_only (hyphen to underscore).

    Both flags are independent of each other and of --demo. Any combination is valid.
  </action>
  <verify>
    <automated>cd /Users/dillera/code/atari800 && python3 puppetmaster.py --help 2>&1 | grep -E "\-\-keys|\-\-connect-only"</automated>
  </verify>
  <done>
    - python3 puppetmaster.py --help output contains "--keys" and "--connect-only"
    - grep "add_argument.*--keys" puppetmaster.py returns a match
    - grep "add_argument.*--connect-only" puppetmaster.py returns a match
    - grep "Sending W\|move up" puppetmaster.py returns a match (output string present in key_sequence)
    - grep "Keys test complete" puppetmaster.py returns a match
    - python3 -m pytest puppetmaster_test.py -v exits 0 (existing tests still pass after edit)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — live emulator control test</name>
  <files>puppetmaster.py</files>
  <action>Human manually tests the complete launch and key-sending flow against the live emulator. No code changes — this task exists solely to gate progression on visual confirmation that W/A/S/D move the player and keys release properly.</action>
  <verify>Human confirms all steps in how-to-verify pass and types "approved".</verify>
  <done>User has approved the checkpoint after observing player movement in response to W/A/S/D key presses.</done>
  <what-built>
    puppetmaster.py — complete CLI controller that launches atari800 with KillZone and sends keyboard inputs via the AI socket. Implemented in plan 01-01 and extended in Task 1 of this plan. Key features:
    - --launch flag: starts ./src/atari800 -ai -xl -run killzone.xex as subprocess
    - --connect-only: connects to already-running emulator and exits after confirmation
    - --keys flag: sends W, A, S, D, Space, Q, Y, N with print output
    - --demo flag: sends W/A/S/D movement sequence
    - Proper key_release after every press (D-09)
  </what-built>
  <how-to-verify>
    You need a built atari800 emulator (./src/atari800) and a KillZone .xex file.

    **Step 1: Build the emulator** (if not already built)
    ```
    cd /Users/dillera/code/atari800
    ./build_ai.sh
    ```
    Expected: "Build complete! Binary: src/atari800"

    **Step 2: Test connection to already-running emulator**
    Start the emulator manually in a separate terminal:
    ```
    ./src/atari800 -ai -xl -run /path/to/killzone.xex
    ```
    Then in another terminal:
    ```
    cd /Users/dillera/code/atari800
    python3 puppetmaster.py --connect-only /path/to/killzone.xex
    ```
    Expected: prints "Connected to emulator at /tmp/atari800_ai.sock" and "Puppetmaster ready. Emulator connected." then exits 0.

    **Step 3: Test --launch mode**
    (Kill any existing emulator instance first)
    ```
    python3 puppetmaster.py --launch --connect-only /path/to/killzone.xex
    ```
    Expected: emulator SDL window appears with KillZone loaded, terminal prints connection confirmation, then exits.

    **Step 4: Test key inputs**
    With emulator running and KillZone at the main game screen (player visible):
    ```
    python3 puppetmaster.py --launch --keys /path/to/killzone.xex
    ```
    Watch the emulator window. Expected:
    - Terminal prints "Sending W (move up)..." and player moves up on screen
    - Terminal prints "Sending A (move left)..." and player moves left
    - Terminal prints "Sending S (move down)..." and player moves down
    - Terminal prints "Sending D (move right)..." and player moves right
    - Terminal prints "Sending Space (attack)..." (attack action fires, or attempt registered)
    - Q, Y, N keys sent without crashing
    - Terminal prints "Keys test complete."

    **Step 5: Confirm key release (no stuck inputs)**
    After the --keys test completes, verify the player is NOT continuously moving in the window. Stationary player = keys releasing correctly.

    **Step 6: Verify connection failure handling**
    With no emulator running:
    ```
    python3 puppetmaster.py --connect-only /tmp/fake.xex
    ```
    Expected: prints a clean error message to stderr, exits with non-zero exit code. Must NOT produce a Python traceback.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass.
    Or describe what failed (e.g., "player didn't move on W", "launch failed — no window", "got Python traceback on connection failure").
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when the human-verify checkpoint is approved with:
1. --connect-only works against a running emulator (exit 0 + confirmation message)
2. --launch starts the emulator with SDL window visible and connects successfully
3. --keys sends all 8 keys and player visibly moves during W/A/S/D presses
4. Keys release properly (player stationary after sequence ends)
5. Connection failure produces a clean error message, not a Python traceback
</verification>

<success_criteria>
All four Phase 1 ROADMAP success criteria met:
1. Running puppetmaster connects to a running atari800 at /tmp/atari800_ai.sock and confirms connection
2. Running puppetmaster --launch starts atari800 with KillZone using the provided .xex path
3. Puppetmaster sends W/A/S/D key presses that visibly move the player
4. Puppetmaster sends Space and menu keys (Q, Y, N) and releases all keys after pressing
</success_criteria>

<output>
After completion, create `.planning/phases/01-emulator-interface/01-02-SUMMARY.md` documenting:
- Verification results (what was tested, what passed)
- Any issues found and fixed during verification
- Final puppetmaster.py API surface
- Notes for Phase 2 (screen parsing, memory reads)
</output>
