---
phase: 01-emulator-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - puppetmaster.py
autonomous: true
requirements:
  - EMU-01
  - EMU-02
  - EMU-03
  - EMU-04
  - EMU-05
  - EMU-06
  - EMU-07
  - EMU-08
  - EMU-09

must_haves:
  truths:
    - "Puppetmaster connects to a running atari800 instance at /tmp/atari800_ai.sock and receives a pong"
    - "Puppetmaster launches atari800 as a subprocess with -ai -xl -run flags when --launch is passed"
    - "Puppetmaster sends W, A, S, D key presses using AKEY codes (W=46, A=63, S=62, D=58)"
    - "Puppetmaster sends Space (33), Q (47), Y (43), N (35) key presses"
    - "Every key press is followed by a key_release command"
  artifacts:
    - path: "puppetmaster.py"
      provides: "Puppetmaster main script with CLI, controller class, and all key methods"
      exports: ["PuppetmasterController", "main"]
      min_lines: 120
  key_links:
    - from: "puppetmaster.py:PuppetmasterController.press_key"
      to: "atari800_ai.py:Atari800AI.key + key_release"
      via: "direct method calls on self._client"
      pattern: "self\\._client\\.key\\("
    - from: "puppetmaster.py:PuppetmasterController.launch_and_connect"
      to: "subprocess.Popen"
      via: "subprocess.Popen with emulator path and xex args"
      pattern: "subprocess\\.Popen"
---

<objective>
Create puppetmaster.py — the Puppetmaster main script that implements the complete emulator interface layer for Phase 1.

Purpose: Provide a working Python script that can launch or connect to atari800 and send all required keyboard inputs (movement keys, attack, menu keys) with proper press/release behavior.

Output: puppetmaster.py at repo root, importable as a library and runnable as a CLI tool.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-emulator-interface/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts from atari800_ai.py. Executor uses these directly. -->

From atari800_ai.py:

```python
class Atari800AI:
    DEFAULT_SOCKET = "/tmp/atari800_ai.sock"

    # Key code constants
    AKEY_W = 46
    AKEY_A = 63
    AKEY_S = 62
    AKEY_D = 58
    AKEY_SPACE = 33
    AKEY_Q = 47
    AKEY_Y = 43
    AKEY_N = 35

    def __init__(self, socket_path: str = None): ...
    def connect(self) -> 'Atari800AI': ...           # raises ConnectionError on failure
    def disconnect(self): ...
    def __enter__(self) -> 'Atari800AI': ...
    def __exit__(self, exc_type, exc_val, exc_tb): ...
    def key(self, code: int, shift: bool = False) -> bool: ...
    def key_release(self) -> bool: ...
    def run(self, frames: int = 1) -> dict: ...
    def type_char(self, char: str) -> bool: ...      # holds 5 frames, releases, waits 2 frames

def wait_for_emulator(socket_path: str = None, timeout: float = 10.0) -> Atari800AI:
    # Polls socket every 0.1s up to timeout, returns connected client
    # Raises TimeoutError if not available
    ...
```

Connection errors to handle:
- `ConnectionRefusedError` — socket exists but nothing listening
- `FileNotFoundError` — socket file not created yet (emulator still starting)
- `TimeoutError` — raised by wait_for_emulator after timeout
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PuppetmasterController class with launch, connect, and key methods</name>
  <files>puppetmaster.py</files>
  <read_first>
    - /Users/dillera/code/atari800/atari800_ai.py (the library being wrapped — read ALL of it)
    - /Users/dillera/code/atari800/.planning/phases/01-emulator-interface/01-CONTEXT.md (decisions D-01 through D-09)
  </read_first>
  <behavior>
    - PuppetmasterController.__init__(emulator_path, xex_path, socket_path) stores paths, sets self._client = None, self._proc = None
    - PuppetmasterController.connect() calls Atari800AI(socket_path).connect(), stores in self._client, prints "Connected to emulator at {socket_path}"
    - PuppetmasterController.connect() raises SystemExit with message "Error: Could not connect to emulator at {socket_path}" and exit code 1 on ConnectionError/FileNotFoundError (per D-06)
    - PuppetmasterController.launch_and_connect(timeout=15.0) spawns subprocess with [emulator_path, "-ai", "-xl", "-run", xex_path] (per D-05), then calls wait_for_emulator(timeout=timeout) to poll socket
    - PuppetmasterController.launch_and_connect raises SystemExit with message "Error: Emulator did not start within {timeout}s" on TimeoutError
    - PuppetmasterController.press_key(code, hold_frames=5) calls self._client.key(code), then self._client.run(frames=hold_frames), then self._client.key_release() — matching the proven timing from type_char (per D-09)
    - PuppetmasterController exposes named methods: move_up(), move_down(), move_left(), move_right(), attack(), menu_q(), menu_y(), menu_n() — each delegates to press_key with the correct AKEY constant
    - PuppetmasterController.disconnect() calls self._client.disconnect() if connected, terminates self._proc if running
    - PuppetmasterController works as context manager (__enter__/__exit__)
  </behavior>
  <action>
    Create puppetmaster.py at repo root. Use 4-space indentation (Python convention per CONVENTIONS.md). Include module docstring.

    File structure:
    1. Module docstring explaining Puppetmaster's purpose
    2. Imports: sys, subprocess, time, argparse; from atari800_ai import Atari800AI, wait_for_emulator
    3. Constants: DEFAULT_EMULATOR = "./src/atari800", DEFAULT_SOCKET = "/tmp/atari800_ai.sock"
    4. PuppetmasterController class with all methods described in behavior section
    5. main() function (CLI entry point — implemented in Task 2)
    6. if __name__ == "__main__": main()

    Key implementation details:
    - press_key(code, hold_frames=5): calls self._client.key(code), self._client.run(frames=hold_frames), self._client.key_release()
    - move_up() uses AKEY_W = 46, move_left() uses AKEY_A = 63, move_down() uses AKEY_S = 62, move_right() uses AKEY_D = 58
    - attack() uses AKEY_SPACE = 33, menu_q() uses AKEY_Q = 47, menu_y() uses AKEY_Y = 43, menu_n() uses AKEY_N = 35
    - launch_and_connect(): subprocess.Popen([self.emulator_path, "-ai", "-xl", "-run", self.xex_path]), then wait_for_emulator(self.socket_path, timeout=timeout)
    - On ConnectionError/FileNotFoundError in connect(): print the error to stderr, sys.exit(1)
    - On TimeoutError in launch_and_connect(): print the error to stderr, sys.exit(1)
    - __exit__ calls disconnect(), does NOT suppress exceptions (return False)

    Write tests in puppetmaster_test.py (same directory):
    - test_press_key_sends_key_then_run_then_release: mock Atari800AI, assert key(46) called, then run(frames=5), then key_release()
    - test_move_up_uses_akey_w: mock client, call move_up(), assert key called with code=46
    - test_move_left_uses_akey_a: assert code=63
    - test_move_down_uses_akey_s: assert code=62
    - test_move_right_uses_akey_d: assert code=58
    - test_attack_uses_akey_space: assert code=33
    - test_menu_q_uses_akey_q: assert code=47
    - test_menu_y_uses_akey_y: assert code=43
    - test_menu_n_uses_akey_n: assert code=35
    - test_connect_exits_on_connection_error: mock Atari800AI.connect to raise ConnectionError, assert SystemExit raised
    - test_launch_spawns_subprocess: mock subprocess.Popen and wait_for_emulator, verify Popen called with [emulator_path, "-ai", "-xl", "-run", xex_path]
  </action>
  <verify>
    <automated>cd /Users/dillera/code/atari800 && python3 -m pytest puppetmaster_test.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - puppetmaster.py exists at /Users/dillera/code/atari800/puppetmaster.py
    - puppetmaster_test.py exists with all 11 tests passing
    - grep "class PuppetmasterController" puppetmaster.py returns a match
    - grep "def press_key" puppetmaster.py returns a match
    - grep "def move_up\|def move_down\|def move_left\|def move_right\|def attack\|def menu_q\|def menu_y\|def menu_n" puppetmaster.py returns 8 matches
    - grep "def launch_and_connect" puppetmaster.py returns a match
    - grep "def connect" puppetmaster.py returns a match
    - All pytest tests pass (exit code 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI entry point with --launch, --emulator, and demo mode</name>
  <files>puppetmaster.py</files>
  <read_first>
    - /Users/dillera/code/atari800/puppetmaster.py (must exist from Task 1 — read it before editing)
    - /Users/dillera/code/atari800/.planning/phases/01-emulator-interface/01-CONTEXT.md (decisions D-01 through D-06)
  </read_first>
  <action>
    Add the main() function to puppetmaster.py. It must implement:

    CLI interface using argparse:
    - Positional argument: xex (required) — path to KillZone .xex file (per D-03)
    - Optional --launch flag: boolean, launches emulator as subprocess (per D-01, D-02)
    - Optional --emulator: path to emulator binary, default="./src/atari800" (per D-02)
    - Optional --socket: path to AI socket, default="/tmp/atari800_ai.sock"
    - Optional --demo: boolean flag, if set runs a quick key demo after connecting

    Behavior:
    1. Parse args
    2. Create PuppetmasterController(emulator_path=args.emulator, xex_path=args.xex, socket_path=args.socket)
    3. If args.launch: call controller.launch_and_connect()
    4. Else: call controller.connect()
    5. Print "Puppetmaster ready. Emulator connected." after successful connection
    6. If args.demo: run demo sequence — call move_up(), run 10 frames, move_down(), run 10 frames, move_left(), run 10 frames, move_right(), run 10 frames — print "Demo complete." after
    7. Call controller.disconnect() on exit (use try/finally)

    Help text must include:
    - Program description: "Puppetmaster: Control the KillZone Atari client via AI socket"
    - xex help: "Path to KillZone .xex file to run"
    - --launch help: "Launch atari800 emulator before connecting"
    - --emulator help: "Path to atari800 binary (default: ./src/atari800)"
    - --demo help: "Send demo key sequence after connecting"

    Usage examples that must work:
    - python3 puppetmaster.py killzone.xex  (connect to already-running emulator)
    - python3 puppetmaster.py --launch killzone.xex  (launch then connect)
    - python3 puppetmaster.py --launch --emulator /usr/local/bin/atari800 killzone.xex
    - python3 puppetmaster.py --launch --demo killzone.xex
  </action>
  <verify>
    <automated>cd /Users/dillera/code/atari800 && python3 puppetmaster.py --help 2>&1 | grep -E "killzone|launch|emulator|demo|socket"</automated>
  </verify>
  <done>
    - python3 puppetmaster.py --help exits 0 and output contains "launch", "emulator", "demo", and "socket"
    - python3 puppetmaster.py --help output contains "KillZone" or "killzone" (case-insensitive)
    - grep "def main" puppetmaster.py returns a match
    - grep "argparse" puppetmaster.py returns a match
    - grep "add_argument.*--launch" puppetmaster.py returns a match
    - grep "add_argument.*--emulator" puppetmaster.py returns a match
    - python3 -c "from puppetmaster import PuppetmasterController" exits 0 (importable as library)
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full file:

1. `python3 puppetmaster.py --help` exits 0 and shows all flags
2. `python3 -c "from puppetmaster import PuppetmasterController; c = PuppetmasterController('./src/atari800', 'game.xex'); print('OK')"` exits 0
3. `python3 -m pytest puppetmaster_test.py -v` all tests pass
4. `grep -c "def " puppetmaster.py` returns at least 12 (class methods + main)
5. `python3 puppetmaster.py` without args exits non-zero with usage error (argparse enforces required xex)
</verification>

<success_criteria>
- puppetmaster.py exists and is importable
- PuppetmasterController has connect(), launch_and_connect(), disconnect(), press_key(), move_up/down/left/right(), attack(), menu_q/y/n() methods
- CLI accepts: xex (positional, required), --launch, --emulator, --socket, --demo
- All 11 unit tests pass
- Key codes match D-08: W=46, A=63, S=62, D=58, Space=33, Q=47, Y=43, N=35
- key_release() called after every key press (per D-09)
- Connection failure exits with non-zero code and clear error message (per D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/01-emulator-interface/01-01-SUMMARY.md` documenting:
- Files created: puppetmaster.py, puppetmaster_test.py
- Key decisions implemented (D-01 through D-09)
- PuppetmasterController API surface
- Any implementation choices made under Claude's discretion
</output>
